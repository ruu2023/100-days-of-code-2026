<%# Append/Prepend the new memo to the list %>
<%= turbo_stream.prepend "memos", partial: "day006/memos/memo", locals: { memo: @memo } %>

<%# Reset the form %>
<%= turbo_stream.update "new_memo_form", partial: "day006/memos/form", locals: { memo: Day006::Memo.new } %>
